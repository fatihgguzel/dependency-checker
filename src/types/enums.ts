export enum GitProviders {
  GITHUB = 'github',
}

export enum DependencyFilePaths {
  JAVASCRIPT = 'package.json',
  PHP = 'composer.json',
}

export enum DependencyRegistries {
  JAVASCRIPT = 'npm',
  PHP = 'packagist',
}
